[project]
name = "data-quality-observatory"
version = "0.1.0"
description = "A dependency-free, auditable CSV data-quality profiler."
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
