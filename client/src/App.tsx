/**
 * Evidence Ledger design system: warm paper, graphite type and Mineral Teal signals.
 * The app keeps one focused route so the data story is immediately visible to a reviewer.
 */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
